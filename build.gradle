// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    ext.min_sdk_version = 21
    ext.target_sdk_version = 29
    ext.compile_sdk_version = 29
    ext.build_tool_version = "30.0.0"
    ext.kotlin_version = "1.3.72"   // https://blog.jetbrains.com/kotlin/category/releases/

    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.0.0"    // https://developer.android.com/studio/releases/gradle-plugin
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}